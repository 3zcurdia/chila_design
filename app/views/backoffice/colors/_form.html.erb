<%= form_with(model: [:backoffice, color]) do |form| %>
  <% if color.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(color.errors.count, "error") %> prohibited this color from being saved:</h2>

      <ul>
        <% color.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, class: "label" %>
    <div class="control">
      <%= form.text_field :name, class: "input is-horizontal" %>
    </div>
  </div>

  <div class="field">
    <%= form.label :value, class: "label" %>
    <div class="control">
      <%= form.text_field :value, class: "input is-horizontal" %>
    </div>
  </div>

  <div class="control">
    <%= form.submit class: "button is-primary is-normal" %>
  </div>
<% end %>
